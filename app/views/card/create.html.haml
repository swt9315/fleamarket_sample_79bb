-# クレジットカード登録後の登録完了しましたページ
.New__content
  .New__content__topic
    .New__content__topic__logo
      = link_to root_path do
        -# = image_tag “logo.png”, alt: “FURIMA：“, class: "New__content__topic__logo__img"
    .New__content__topic__info
      %p.registration__title
        登録が完了しました。
    .New__content__topic__btn
      = link_to "マイページに戻る", user_path(current_user.id), class: "New__content__topic__btn__push"
      = link_to "登録カード一覧", card_path(current_user.id), class: "New__content__topic__btn__push"